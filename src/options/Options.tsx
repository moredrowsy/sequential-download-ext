import browser from 'webextension-polyfill';
import React, { Component } from 'react';

import './Options.css';

class Options extends Component {
  render() {
    return <div className='Options'>Options Page</div>;
  }
}

export default Options;
